server {
    listen 80 default_server;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Vue Router uses HTML5 history mode; deep links must serve index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Query API lives in another container. The query server's routes
    # already begin with /api/, so we pass through verbatim.
    location /api/ {
        proxy_pass         http://query:8082;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }
}

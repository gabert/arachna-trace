server_port=8082
clickhouse_url=http://clickhouse:8123
clickhouse_database=deepflow
clickhouse_user=deepflow
clickhouse_password=deepflow
# UI is served from the same nginx that proxies /api/ to this service,
# so the browser-visible Origin is http://localhost:8080. Allow that.
cors_origin=http://localhost:8080
